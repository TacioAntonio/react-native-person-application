import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    inputWrapper: {
        gap: 2,
        marginBottom: 15,
    },
    input: {
        height: 40,
        borderWidth: 1,
        paddingLeft: 6,
        borderRadius: 12,
        borderColor: '#927afb'
    },
    errorText: {
        color: 'red',
        fontSize: 12
    }
});
