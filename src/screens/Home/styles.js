import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 12
    },
    formWrapper: {
        backgroundColor: '#ffffff',
        padding: 26,
        borderRadius: 26
    },
    title: {
        color: '#846ce7',
        fontSize: 22,
        fontWeight: 'bold',
        marginBottom: 12
    }
});
